package com.example.workmanagerguide

object WorkerKeys {
    const val ERROR_MSG = "errorMsg" // key of error message if task failed
    const val IMAGE_URI = "imageUri" // key to path of downloaded file if task was successful
    const val FILTER_URI = "filterUri" // key to path of file when the filter was applied
}